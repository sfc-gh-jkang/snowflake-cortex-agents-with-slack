# ==============================================================================
# Local Development Environment Variables
# ==============================================================================
# Copy this file to .env and fill in your actual values
# DO NOT commit .env to git - it's in .gitignore
#
# Usage: cp .env.example .env
# Then edit .env with your actual credentials
# ==============================================================================

# ------------------------------------------------------------------------------
# Slack Configuration
# ------------------------------------------------------------------------------
# Get these from https://api.slack.com/apps
# Bot Token: OAuth & Permissions > Bot User OAuth Token (starts with xoxb-)
# App Token: Basic Information > App-Level Tokens (starts with xapp-)
SLACK_BOT_TOKEN=xoxb-your-bot-token-here
SLACK_APP_TOKEN=xapp-your-app-token-here

# ------------------------------------------------------------------------------
# Snowflake Connection (Local Development)
# ------------------------------------------------------------------------------
# Account identifier (e.g., ORG-ACCOUNT)
ACCOUNT=your-account-identifier

# Snowflake host (lowercase with hyphens, e.g., org-account.snowflakecomputing.com)
HOST=your-account.snowflakecomputing.com

# Your Snowflake username
DEMO_USER=your-username

# Role to use for operations
DEMO_USER_ROLE=your-role

# Warehouse for compute
WAREHOUSE=your-warehouse

# Optional: Default warehouse for SPCS (defaults to CORTEX_SLACK_BOT_WH if not set)
DEFAULT_SPCS_WAREHOUSE=CORTEX_SLACK_BOT_WH

# ------------------------------------------------------------------------------
# Cortex Agent Configuration
# ------------------------------------------------------------------------------
# Cortex Agent endpoint URL
# Format: https://your-account.snowflakecomputing.com/api/v2/databases/DB_NAME/schemas/SCHEMA_NAME/agents/AGENT_NAME:run
AGENT_ENDPOINT=https://your-account.snowflakecomputing.com/api/v2/databases/snowflake_intelligence/schemas/agents/agents/your-agent-name:run

# Personal Access Token (PAT) for Cortex Agent API authentication
# Generate in Snowsight: User menu > My Profile > Personal Access Tokens
PAT=your-personal-access-token

# ------------------------------------------------------------------------------
# Optional: SPCS Development Variables (usually not needed locally)
# ------------------------------------------------------------------------------
# These are primarily used when testing SPCS-specific functionality locally
# SNOWFLAKE_HOST=your-account.snowflakecomputing.com
# SNOWFLAKE_ACCOUNT=YOUR-ACCOUNT-IDENTIFIER
# SNOWFLAKE_DATABASE=CORTEX_SLACK_BOT_DB
# SNOWFLAKE_SCHEMA=APP_SCHEMA
# SNOWFLAKE_WAREHOUSE=CORTEX_SLACK_BOT_WH
# SNOWFLAKE_PORT=443
